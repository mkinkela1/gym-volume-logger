import React from 'react';
import {Text, View} from 'react-native';

const Session = () => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text style={{color: '#efefef'}}>Session Screen</Text>
    </View>
  );
};

export default Session;
