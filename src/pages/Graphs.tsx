import React from 'react';
import {Text, View} from 'react-native';

const Graphs = () => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text style={{color: '#efefef'}}>Graphs Screen</Text>
    </View>
  );
};

export default Graphs;
